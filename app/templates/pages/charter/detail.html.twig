{# Remove comments to add a view app to the template #}
{% extends 'base.html.twig' %}

{% set page_title %}{{ 'Charter'|trans }}{% endset %}

{% block body_class %}layout--fullscreen layout--compact{% endblock %}

{% block main %}
    <charter-view-app id="charter-view-app"
                   init-urls="{{ urls }}"
                   init-data="{{ data }}"
    />
{% endblock %}

{% block javascript %}
    {{ parent() }}
    {{ encore_entry_script_tags('charter-view') }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('charter-view') }}
{% endblock %}
